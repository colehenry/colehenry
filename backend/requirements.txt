fastapi>=0.115
uvicorn[standard]>=0.32
# Multipart form parsing for the Kobo import upload (POST /language/import/kobo).
python-multipart>=0.0.9
sqlalchemy>=2.0
alembic>=1.14
pydantic>=2.9
pydantic-settings>=2.6
psycopg[binary]>=3.2
authlib>=1.3
pyjwt>=2.9
itsdangerous>=2.2
httpx>=0.27
# YAML frontmatter parsing for brain vault notes (app/services/brain.py).
pyyaml>=6.0
fsrs>=5.0
cloudinary>=1.41
# Runtime conjugation for wiki lookups of unsaved verbs (POST /language/verbs,
# GET /language/wiki). Heavy (ML deps) — remove this line to slim the image;
# those endpoints degrade gracefully when it's absent.
verbecc>=1.9
# Lightweight FR/ES headword resolution for conjugated forms captured from
# Texts/Wiki. Language data loads lazily and the service degrades gracefully.
simplemma>=1.1
