"""Curated Month-1 French curriculum (source of truth: french_month1_study_system_v2.md).

Everything here is hardcoded on purpose — the curriculum is curated and
inspectable; only *practice* is generated. Sync into Postgres with
`python -m app.seed_french` (idempotent) or File → Sync curriculum in the UI.
"""
